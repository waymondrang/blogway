# src/lambda

This directory contains the source code for Lambda functions. It is referenced as a relative path in template.yaml, which SAM will package and upload into a managed S3 bucket as part of the `sam build` and `sam deploy` process.
